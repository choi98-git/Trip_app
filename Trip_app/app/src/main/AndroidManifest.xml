<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trip_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trip_app">
        <activity android:name=".TaeJong"></activity>
        <activity android:name=".KamCheon" />
        <activity android:name=".Busan" />
        <activity android:name=".LasVegas" />
        <activity android:name=".GrandCanyon" />
        <activity android:name=".USA" />
        <activity android:name=".Domestic" />
        <activity android:name=".Overseas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>